interface:
  display_name: "Simulator Development"
  short_description: "Guardrails for col.sim simulator changes"
  default_prompt: "Use $sim-development to implement or debug a col.sim simulator change while following the repo's architecture and validation rules."

policy:
  allow_implicit_invocation: true
